pipeline {
    agent any

    options {
        skipDefaultCheckout(true)   // avoids checkout logs
    }

    stages {
        stage('Run') {
            steps {
                script {
                    // Do nothing or minimal command
                }
            }
        }
    }
}
